# 参与贡献

没有仓库写入权限也可以参与改进。这个项目采用“公开候选、维护者审核”的方式：AI 可以帮助你把想法整理成规范的改进候选，但不会直接替你修改公共仓库，也不代表候选已经被项目接受。

## 两种提交方式

### 1. 提交改进候选

如果你只有一个想法、使用中遇到的问题，或者一段由 AI 整理出的建议，可以使用仓库的“改进候选” Issue 模板。提交时尽量说明：

- 在什么场景下遇到了什么问题；
- 当前行为为什么不合适；
- 希望调整什么，以及哪些内容不应改变；
- 这项改动可能影响哪些文件或使用方式；
- 怎样判断调整后的结果是正确的；
- 还有哪些问题没有确认。

维护者会公开审核，并决定接受、调整、暂缓或拒绝候选。

### 2. Fork 后提交 Pull Request

如果你已经准备好具体修改，可以：

1. Fork 本仓库；
2. 在自己的 Fork 中创建分支并完成修改；
3. 根据 `evals/cases.md` 检查行为；
4. 如果改动了 `skill/intent-debugger/SKILL.md`，运行 Skill 校验器；
5. 向本仓库的 `main` 分支提交 Pull Request。

Pull Request 模板会要求说明修改依据、影响范围、未决问题和验证结果。维护者审核通过后再合并，贡献者不需要本仓库的直接写入权限。

## 让 AI 先整理候选

可以把下面这段话交给支持本项目 Skill 的 AI：

```text
请把下面的反馈整理成一个 Intent Debugger 改进候选，先不要修改仓库。

候选需要包含：问题场景、修改目标、建议改动、保持不变的边界、可能影响、未决问题和验收方法。请区分用户已经提供的事实、你的合理推断和仍需确认的内容，不要把候选写成已经确定的项目决定。

我的反馈：
（在这里填写）
```

整理完成后，检查内容是否符合你的真实意思，再通过 Issue 或 Pull Request 提交。

## 基本要求

- 不提交密码、令牌、私人对话或其他敏感信息；
- 不把 AI 的推断写成用户或维护者已经确认的事实；
- 核心方法保持平台中立，Codex、Claude Code、DeepSeek 的差异放在适配器中；
- 不把 Intent Debugger 描述成 Plan 的固定前置步骤；
- 行为发生变化时，同步更新相应示例或验收用例。
